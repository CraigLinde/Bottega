<h1 id = "myheading">Angular Items</h1>

<% @angular_portfolio_items.each do |p_item|%>
  <p><%= p_item.title %></p>
  <p><%= p_item.subtitle %></p>
  <p><%= p_item.body %></p>
  <p><%= image_tag p_item.thumb_image unless p_item.thumb_image.nil? %></p>

  <%end%>
  